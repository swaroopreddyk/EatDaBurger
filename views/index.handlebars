<div class="container">
    <div class="row">
        <div class="col">
            <h2>burgers that are not devoured!</h2>
            <ul>
                {{#each burgers}}
                {{#unless devoured}}
                {{> foods/burger-block devoured=false}}
                {{/unless}}
                {{/each}}
            </ul>
        </div>
        <div class="col">
            <h2>burgers that are devoured!</h2>
            <ul>
                {{#each burgers}}
                {{#if devoured}}
                {{> foods/burger-block devoured=true}}
                {{/if}}
                {{/each}}
            </ul>
        </div>
    </div>
    <h2>Add a burger</h2>
    <form class="create-form">
        <div class="form-group">
            <input type="text" id="burgerName" name="name" placeholder="Burger name">
        </div>
        <button id="submitBurger" type="submit">Add Burger</button>
    </form>
</div>